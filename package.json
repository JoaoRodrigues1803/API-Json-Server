{
  "name": "api-jsonserver",
  "version": "1.0.0",
  "description": "API JSON Server hospedada no Render",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
